version: "3.8"

services:
  copilot-api:
    build: .
    ports:
      - "8080:8080"
    environment:
      - GH_TOKEN={GH_TOKEN}
    volumes:
      - ./copilot-data:/root/.local/share/copilot-api
    restart: unless-stopped
